[tool.poetry]
name = "genai-project-app"
version = "0.1.0"
description = ""
authors = ["fahima-a <fahima.ahm@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
